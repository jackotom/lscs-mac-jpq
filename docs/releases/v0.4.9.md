# v0.4.9 记牌内容消失修复

## 更新

- 修复炉石新会话先写入登录、套牌日志，稍后才写入对局日志时，记牌器可能一直停在“等待开局”的问题。
- 自动监听始终保留正确的检查范围，对局日志出现后会在下一次检查时接上。
- 手动选择 `Decks.log` 或 `LoadingScreen.log` 后，同一会话里的对局日志稍后出现也能自动恢复。
- 切换过程中使用已经确认的新会话，避免再次扫描时误回到旧对局。

## 版本

- 应用内版本、安装包版本和依赖锁文件统一为 `0.4.9`。

## 验证

- 使用故障当天的日志时间线复现：旧对局存在，新会话先出现登录和套牌日志，之后才出现对局日志。
- 覆盖自动发现、手动选择两个等待日志、同一路径增长不重放、暂停后不切换和二次扫描回旧对局等情况。
- 当前工作区 148 个测试文件中 1193 项通过、1 项跳过；类型检查和正式构建通过。
- 隔离发布包 143 个测试文件中 1174 项通过、1 项跳过，另有 76 项窗口与设置专项测试通过。
- 7 阶段发布验收全部通过：签名打包、代表性日志回放、窗口截图、组件与权限检查、安装包启动均正常。
- 已覆盖安装到 `/Applications/炉石记牌器.app`；安装版本为 `0.4.9`，安装内容与验收包一致，正常启动后又用隔离对局回放确认真实手牌可见。
- 原 `0.4.8` 应用保留在 `/Applications/炉石记牌器.app.v0.4.8-backup-20260814`，需要时可以回退。
