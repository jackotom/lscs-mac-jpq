# v0.5.8 关联牌补全

## 用户可见变化

- “预言师”等随机召唤固定费用随从的卡牌，现在会显示卡库可见的对应费用候选牌。
- 普通悬浮简介先显示前 12 张理论候选；固定卡牌说明后可继续分批查看完整候选池。
- 已推断出的搭配牌会并入关联牌展示；固定关联、理论候选和本局实际结果仍保持分区。
- 有理论候选或实际结果时不再同时显示误导性的“关联牌（0）”。

## 版本一致性

- 应用界面、安装包和依赖锁文件版本统一为 `0.5.8`。
