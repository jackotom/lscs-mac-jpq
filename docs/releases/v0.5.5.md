# v0.5.5 跨日志目录选牌恢复修复

## 更新

- 修复记牌器已连接旧日志目录后，不再寻找新会话选牌日志的问题。
- 炉石稍后在另一个日志目录创建选牌记录时，记牌器会自动发现并切换。
- 竞技场选牌评分无需退出重开记牌器即可恢复显示。

## 版本

- 应用界面、安装包和依赖锁文件统一为 `0.5.5`。

## 验证范围

- 覆盖旧日志目录先存在、新会话选牌日志稍后在另一个目录出现的启动顺序。
- 覆盖现有对局日志发现、牌库监听和竞技场选牌监听。
- 覆盖应用构建、打包及安装版启动检查。
