# v0.7.4 悬浮窗前台识别修复

## 用户可见变化

- macOS 把炉石前台误报为 `Battle.net` 时，记牌器会结合真实最前窗口纠正识别，悬浮窗不再消失。
- 只有炉石确实拥有最前普通窗口时才会纠正，不会把 Battle.net 启动器误判为游戏。

## 安装包要求

- 应用界面、安装包和依赖锁文件版本统一为 `0.7.4`。
- 正式安装包仍需通过 Developer ID 签名、Apple 公证、票据装订与完整发布检查。
