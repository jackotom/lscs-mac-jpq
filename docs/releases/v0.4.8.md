# v0.4.8 首页顶栏精简

## 更新

- 本版包含 `v0.4.7` 引入的全局可拖动顶部任务栏：首页、设置和其它工作台页面共用同一条。
- 去掉首页顶栏中可见的“首页”文字，中央保持为完整的窗口拖动区。
- 工作台顶栏仍会显示“实时对局”等当前页面标题，方便辨认所在位置。
- 左侧 macOS 系统按钮安全区、中央拖动区和右侧窗口按钮的交互保持不变。

## 版本

- 应用内版本、安装包版本和依赖锁文件统一为 `0.4.8`。

## 验证

- 41 项顶栏与页面回归测试通过：首页标题为空、中央拖动区保留，进入工作台后仍显示当前页面标题。
- 完整测试通过：143 个测试文件中 1173 项通过、1 项跳过；另有 76 项窗口与设置专项测试通过。
- 类型检查、正式构建、Apple 芯片签名打包、压缩包校验和安装包启动全部通过。
- 已覆盖安装到 `/Applications/炉石记牌器.app`，安装版本与构建版本均为 `0.4.8`，签名和 Apple 芯片架构校验通过。
- 已用真实鼠标验证：未激活的首页窗口可从空白顶栏直接拖动；工作台、悬浮窗设置、返回首页和最小化恢复均正常，按钮点击不会带动窗口。
