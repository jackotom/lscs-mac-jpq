# 更新记录

## v0.6.5 竞技场跨局与重选识别修复

- 竞技场第一局结束后保留同一轮完整牌库，重复的局间简略快照不再覆盖已确认 30 张牌。
- 构筑画面只识别到模式、没有识别到明确套牌时，不再清空仍有效的竞技场状态。
- 炉石窗口截图增加超时恢复；一次截图卡住后，后续竞技场三选一会继续自动识别。
- 地下竞技场选完新牌进入弃牌阶段后，候选池选取率与影响面板保持显示，直到最终牌库确认。
- 对手已确认手牌改为与牌库、手牌等分组一致的折叠按钮，固定放在“移除”下一行、“当前 / 历史”切换栏上方。
- 应用界面、安装包和依赖锁文件版本统一为 `0.6.5`。

## v0.6.3 权限管理与悬浮窗修复

- 二级工作台第一行新增权限管理，只展示软件真实需要的屏幕录制权限。
- 自动识别会先检查录屏权限；未授权时不再反复触发系统请求，只能由用户主动点击授权。
- 主动授权的首次系统调用若临时失败，本次运行内允许用户再次点击重试。
- 对手悬浮窗保留回合与行动方信息；窗口拉高时固定信息行不再被拉伸，新增空间只交给牌表。
- 对局结束或停留在大厅、竞技场选牌、等待开局时，场攻计数器会立即隐藏；真实场攻页面在未确认对局前也不会闪现“0”。
- 应用界面、安装包和依赖锁文件版本统一为 `0.6.3`。

## v0.6.0 Apple 公证发行版

- 安装包改用 Developer ID 正式分发签名，启用 Hardened Runtime 和可信时间戳。
- 安装包通过 Apple 公证服务检查，并把公证票据装订到应用内，离线时也能由 macOS 验证。
- 竞技场胜负按对局标识持久去重；应用重启后重放同一局日志不会重复累计。
- 保留 v0.5.11 的对手手牌、竞技场档案与收藏中心全部更新。
- 应用界面、安装包和依赖锁文件版本统一为 `0.6.0`。

## v0.5.11 对手手牌、竞技场档案与收藏中心

- 对手悬浮窗新增手牌抽取回合、已知卡牌、创建、锻造、增益和可信回合状态；没有可靠开始时间时不显示虚假倒计时。
- 新增竞技场中心：自动建档、持续更新牌组、胜负与奖励、套牌评分、本机高胜档案、可信留牌统计，以及 JSON 导入导出。
- 新增收藏中心：已记录卡牌、缺卡、开包历史、分系列保底、卡背、英雄皮肤和硬币，支持结构化导入和本机录入。
- 保底只根据本软件确认的开包记录重算，并始终标明“从本次记录起”；手动录入不会冒充日志数据。
- 竞技场与收藏数据使用本机原子存储，损坏档案不会阻止实时记牌；辅助悬浮窗不能调用长期数据写入接口。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.11`。

## v0.5.10 小悬浮窗拖动修复

- 拖动奥秘、我方场攻、对手场攻或智能计数小窗时，主记牌器和其它悬浮窗不再一起消失。
- 拖动期间及松手后的前台切换会被识别为记牌器自身操作，不要求再次点击游戏恢复。
- 切换到 Finder 等其它应用时仍按“仅在游戏内显示”设置正常隐藏。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.10`。

## v0.5.9 米尔牢斯全局效果修复

- 打出“米尔牢斯·法力风暴”后，记牌器会把它显示在本方“影响全局”区域；竞技场三列表界面也会显示在牌表上方。
- 同一条出牌日志重复出现时只记录一次；新对局或对局结束后自动清除。
- 本方与对手的全局效果继续严格分开。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.9`。

## v0.5.8 关联牌补全

- “预言师”等随机召唤固定费用随从的卡牌，现在会显示卡库可见的对应费用候选牌。
- 普通悬浮简介先显示前 12 张理论候选；固定卡牌说明后可继续分批查看完整候选池。
- 已推断出的搭配牌会并入关联牌展示；固定关联、理论候选和本局实际结果仍保持分区。
- 有理论候选或实际结果时不再同时显示误导性的“关联牌（0）”。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.8`。

## v0.5.7 对手已使用卡图修复

- 对手记牌器“已使用”历史现在显示卡图。
- 图源加载失败时按备用源继续尝试；全部失败时仍保留卡名文字。
- 原有回合、确认状态和列表滚动保持不变。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.7`。

## v0.5.6 奥秘职业识别修复

- 修复对手英雄职业与单个奥秘日志中的明确职业不一致时，奥秘助手无法生成正确候选的问题，包括圣骑士奥秘未显示的情况。
- 单个奥秘带有明确 `CLASS` 时，按该奥秘的职业生成候选；该信息未知时仍回退到对手英雄职业。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.6`。

## v0.5.5 跨日志目录选牌恢复修复

- 修复记牌器已连接旧日志目录后，不再寻找新会话选牌日志，导致竞技场评分一直不显示的问题。
- 炉石稍后在另一个日志目录创建选牌记录时，记牌器会自动发现并切换，不需要退出重开。
- 增加跨目录延迟出现选牌日志的回归覆盖，防止启动顺序变化再次造成监听卡住。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.5`。

## v0.5.4 首次启动自动连接修复

- 修复先打开记牌器、后启动炉石时，旧的根目录 `Player.log` 会让自动发现提前停止的问题。
- 即使启动时只有 `Player.log`，记牌器也会继续检查新日志；游戏生成 `Power.log` 后自动切换并开始记录。
- 增加真实启动顺序回归测试，覆盖“记牌器先开、游戏后开、无需重启”的完整恢复过程。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.4`。

## 未发布 · 许可说明

- 项目代码采用 `PolyForm Noncommercial License 1.0.0`：允许许可规定的非商业使用；任何商业使用必须事先取得版权所有者的书面授权。
- 项目改称“源码可见、非商业许可”，不再把限制商业使用的许可称为开源许可。
- 安装包会同时携带项目许可和第三方组件许可说明；第三方内容继续遵循其原有许可。

## v0.5.3 悬浮计数器交互修复

- 智能计数圆片接入鼠标操作和拖动，不再始终穿透、无法抓取。
- 我方与对手场攻补齐首次移入唤醒；短按小于 4 像素不移动，松手、取消和抓取丢失后立即恢复穿透。
- 每个智能计数器按自身编号独立保存位置，定时刷新、隐藏再显示、重启和显示器变化后不会互相串位或弹回默认位置。
- 穿透恢复失败会持续安全重试；鼠标快速重新移入、重复离开事件和窗口异常关闭不会留下错误交互状态。
- 默认计数器与场攻按钮、其它计数器保持间距；小屏、负坐标副屏和带 Dock 的工作区自动换行。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.3`。

## v0.5.2 克尔苏加德复活数量修复

- “天定之灾克尔苏加德”仍在牌库、尚未公开自身日志计数时，改为按同侧已死亡的不稳定的骷髅实时统计。
- 卡牌日志计数出现后以游戏提供的真实值为准，支持 0，并保持我方、对手严格隔离。
- 重复的日志副本不会重复计数，新对局会清空旧数量并从 0 开始。
- 暂时无法确认数量时显示“复活数量未知”，不再保留错误的空白“复活 个”。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.2`。

## v0.5.1 奥秘窗口交互修复

- 修复奥秘助手真实窗口开启鼠标穿透后，问号和标题可能无法接收点击的问题。
- 问号和标题都支持短按收起、展开与拖动移动；拖动超过 4 像素后不会误触收起或展开。
- 折叠后的 `44×44` 窗口全部是有效点击和拖动区域，问号视觉大小保持不变。
- 点击、拖动结束或取消后都会恢复鼠标穿透，奥秘候选列表继续不阻挡炉石操作。
- 奥秘窗口位置会随折叠状态和应用重启保存，折叠、展开时保持同一左上锚点。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.1`。

## v0.5.0 悬浮窗操作更新

- 奥秘助手左上角问号可以收起整个候选列表，再次点击恢复；折叠状态会在刷新和重启后保留。
- 我方与对手场攻圆钮可以分别拖动，位置会在刷新、重启和显示器变化后恢复，并始终保留在屏幕安全范围内。
- 只有问号和场攻圆钮在操作时接收鼠标，其余透明区域继续穿透，不遮挡炉石点击。
- 修复奥秘助手重新展开后窗口高度被压扁、候选列表显示不完整的问题。
- 应用界面、安装包和依赖锁文件版本统一为 `0.5.0`。

## v0.4.9 记牌内容消失修复

- 修复炉石启动后新日志先出现、对局日志稍后出现时，记牌器可能一直停在“等待开局”并把牌表显示为 0 的问题。
- 自动监听会持续检查正确的日志范围；真正的对局日志出现后，下一次检查就会接上，不需要重启记牌器。
- 手动选择 `Decks.log` 或 `LoadingScreen.log` 时，同一局稍后出现的对局日志也会自动接上。
- 应用界面、安装包和依赖锁文件版本统一为 `0.4.9`。

## v0.4.8 首页顶栏精简

- 包含 `v0.4.7` 引入的全局可拖动顶部任务栏，首页、设置和其它工作台页面继续共用同一条。
- 去掉首页顶栏中可见的“首页”文字，中央拖动区保持完整，界面更干净。
- 工作台页面继续显示当前标题，最小化、进入工作台和返回首页按钮行为不变。
- 应用界面、安装包和依赖锁文件版本统一为 `0.4.8`。

## v0.4.7 全局可拖动顶部任务栏

- 首页、设置和其它工作台页面统一使用一条独立的顶部任务栏，切换页面时不会重复出现。
- 任务栏左侧为 macOS 红黄绿系统按钮保留安全区域，中央大面积区域可用鼠标拖动窗口。
- 右侧最小化、进入工作台和返回首页保持独立点击区，不会触发窗口拖动。
- 首页品牌与服务状态移到任务栏下方，窗口控制与产品信息分层更清楚。
- 应用界面、安装包和依赖锁文件版本统一为 `0.4.7`。

## v0.4.6 地下竞技场与牌表状态修复

- 地下竞技场点击“开始”前就显示当前 31–35 张候选牌，不再空白；顶部明确标注“候选 · 最终30”。
- 匹配后收到准确的 30 张套牌记录时自动替换候选牌，不使用旧套牌冒充当前套牌。
- 对局结束后继续保留当前构筑或竞技场牌表，并清空本局手牌、场面、历史和计数，下一局从完整牌表开始。
- 奥秘助手改为紧凑单列卡图布局，窗口按候选数量自动调整，继续保持鼠标穿透和不抢焦点。
- 应用界面、安装包和依赖锁文件版本统一为 `0.4.6`。

## v0.4.2 独立悬浮窗与智能计数更新

- 我方场攻、对手场攻、奥秘预测和智能计数拆成独立悬浮窗，可分别打开和关闭。
- 奥秘小窗加入真实卡图与名称，候选采用双列紧凑布局，并按数量自动调整高度；图片不可用时显示稳定占位。
- 智能计数按当前套牌和实战状态自动出现，每个计数器可单独隐藏；首批覆盖龙牌进度、虚空灵魂使用次数、法术次数、不同派系和不同种族等可信规则。
- 龙牌计数支持动态门槛、全部种族、排除触发牌自身和晚揭示回填；关键牌只统计真实打出，不把直接召唤误算成使用。
- 独立悬浮窗保持鼠标穿透、不抢游戏焦点，关闭一个窗口不会影响其它窗口。

## v0.4.1 可靠性更新

- 修复竞技场附带牌编号含小写后缀时显示内部编号的问题；“侏儒光环”和“梅卡托克的光环”现在会显示中文名、费用、卡面和真实影响值。
- 套牌识别会明确区分炉石日志确认、画面确认和本局推断；多个相似候选并存时不再静默选错套牌。
- 每次开始监听都会创建独立会话，旧日志读取、旧画面识别和旧评分结果不能污染新一局。
- 模糊画面识别需要连续确认，暂时少识别一张时保留上一次完整结果，减少竞技场三选一闪烁和退化。
- 卡牌与竞技场评分缓存改为安全写入、备份和损坏恢复，更新失败时优先保留最后一份有效数据。
- 增加异常退出记录和日志回放覆盖，强化重复开局、乱序事件、随机分块和长时间日志处理。

## v0.4.0 重大更新

- 全新白色主界面：资讯、热门卡组、竞技场胜率排行、最近对局和模式统计集中展示，设置统一从右上角齿轮进入。
- 全部功能页改为统一的白色工作台，并修复卡牌详情、关联牌、提示区和列表中残留的黑色背景及低对比度文字。
- 悬浮窗新增独立浅色/深色外观；默认仅在炉石游戏内显示，打开记牌器本体时不会自行弹出。
- 修复点击开关、拖动位置与透明度时主窗口隐藏、下沉或跳走的问题；左侧菜单固定，不再随内容滚动。
- 首页资讯改为中文并显示新闻图片；热门卡组与竞技场排行使用可核验数据，读取失败时明确提示，不伪造结果。
- 加强竞技场传说团队与长名称随从识别，最后一组选择和 29/30 场景会继续识别。

## v0.3.16

- 修复选到传说团队后，程序把三张牌错算成四张，导致 29/30 时提前停止识别的问题。
- 最后一组三选一现在会继续识别；自动悬浮窗仍使用无焦点显示，不会抢走炉石操作。

## v0.3.15

- 主界面按冰蓝桌面助手参考图重做，首页、实时对局、卡牌资料、卡组工具、对局记录和设置使用统一深蓝配色。
- 保留原有日志监听、记牌、竞技场、卡牌资料、历史记录和设置功能；品牌、卡牌费用与业务状态继续使用原有语义颜色。
- 地下竞技场重选新增容错匹配：唯一可信的传说团队可在多处识别错字时正常匹配，无法确认时仍不显示或伪造选取率。

## v0.3.14

- 地下竞技场重选时，每选中一张新牌就立即进入记牌器，不再等点击“完成”。
- 尚未选完或最终牌库还没确认时，用“待确认重选牌”保留空位，不把旧牌或 31–35 张候选池冒充正式牌库。
- 精确的最终 30 张到达后自动替换临时结果；错 ID 和过期牌库不会覆盖当前重选。

## v0.3.13

- 竞技场三选一和地下竞技场重选时，每张候选牌下方显示“抽到影响 / 对套牌影响 / 选取率 / 高胜选取率”四项真实统计。
- 抽到影响为抽到该牌后的胜率减职业总体胜率；对套牌影响为带该牌的套牌胜率减职业总体胜率。影响值保留两位小数，选取率保留一位百分比。
- 高胜标签按数据实际分桶动态显示：有 6 胜桶时显示“6+胜选取率”，其它桶显示真实门槛，不把其它数据说成 6 胜数据。
- 旧职业评分缓存缺少抽到统计时会在后台刷新；断网仍会用包含原始计数的新缓存重算。缺少任一指标时显示“暂无”，不猜测或拿其它指标替代。

## v0.3.12

- 修复初始牌库简介因分行标签顺序不同而漏卡的问题。
- 本方身份未确认时先等待完整证据，避免把未知事件错记成对手出牌。
- 活跃竞技场拿到精确牌组后原位刷新，不再重置当前对局。
- 自动匹配至少需要两张不同的本方已观察卡，减少误匹配。

## v0.3.11

- 正式安装包只加载内置页面，外部开发地址不能接管应用窗口。
- 非法实时状态不再挡住合法初始化或覆盖最后一次有效数据，恢复后自动清除错误。
- 日志处理失败会从原位置自动重试；界面推送失败不会导致日志重复生效。

## v0.3.10

- “超时空鳍侠”的卡牌简介现在会显示换牌结束后的我的起始手牌。
- 重复起始手牌会正常保留，“幸运币”不会被误算为起始手牌。
- 有起始手牌资料时，不再显示无意义的“关联牌（0）”。

## v0.3.9

- 修复地下竞技场重选第 1–5 张牌期间，原 30 张牌的费用、选取率和卡组影响整列变成问号或横线的问题。
- 重选牌表现在会从 30 张稳定增长到 35 张；最终替换结果确认后再回到精确 30 张，不再提前清空或猜测删牌。
- 卡牌资料和评分稍晚加载时，已确认牌库会自动补全，不需要等待界面切换恢复。

## v0.2.8

- 修复“匣中古神”等随机施法牌详情缺少候选牌的问题，并覆盖全部 22 种同类牌。
- 新增本次实际施放结果：双倍施放按真实 10 张或更多记录，古神再次施放古神时保留嵌套关系。
- 修复手牌已满时，牌从牌库直接烧毁后仍残留在牌库或手牌的问题。
- 大型候选牌池改为分批显示，打开详情不再一次加载数千张牌。
